@../AGENTS.md

<!--
  Nothing below the import. Every rule in AGENTS.md applies to any agent working
  in this repository, and Claude Code discovers this plugin's skills on its own,
  so there is no Claude-specific line to add. Claude Code loads a project
  CLAUDE.md from either ./CLAUDE.md or ./.claude/CLAUDE.md; this repository uses
  the second because `claude plugin validate --strict` warns on a CLAUDE.md at a
  plugin's root. The import is relative to this file, hence `@../AGENTS.md`.
-->
