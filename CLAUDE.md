@AGENTS.md

<!--
  Nothing below the import. Every rule in AGENTS.md applies to any agent working
  in this repository, and Claude Code discovers this plugin's skills on its own,
  so there is no Claude-specific line to add.

  `claude plugin validate` warns that this file "is not loaded as project
  context" — correct, and not the point. It is project context for people and
  agents working on this repository, which is what CLAUDE.md is for. What the
  plugin ships to its users is skills/, and that is where its own guidance
  lives. See the Commands section of AGENTS.md for how to run validate.
-->
